// Auth disabled for now - will add back with database adapter later
// import { authMiddleware } from "@/lib/auth.config";
// export default authMiddleware;

export const config = {
  matcher: [], // No routes protected
};
